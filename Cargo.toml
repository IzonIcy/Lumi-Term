[package]
name = "lumi-term"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1.0"
directories = "6.0"
eframe = { version = "0.35.0", default-features = true }
portable-pty = "0.9"
serde = { version = "1.0", features = ["derive"] }
toml = "0.8"
vt100 = "0.16"

[build-dependencies]
cc = "1.1"

[lints.rust]
dead_code = "allow"
[lints.clippy]
collapsible_if = "allow"

